#include <stdio.h>
/**
 * main - entry point
 *
 * Return: alway return 0
 */
int main(void)
{
	char a;

	for (a = 'a'; a <= 'z' ; a++)
		putchar(a);
	for (a = 'A'; a <= 'Z' ; a++)
		putchar(A);
	putchar('\n')
		return (0);
}


