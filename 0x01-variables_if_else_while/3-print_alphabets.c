#include <stdio.h>
/**
 * main - emtry point
 *
 * Return: always returns 0
 */
{
	char a;

	for (a = 'a'; a <= 'z' ; a++)
		putchar(a);
	for (a = 'A'; a <= 'Z' ; a++)
		putchar('\n');
	return (0);
}


